# Nano CLI Configuration
# Copy this file to .env and customize as needed

# ===========================================
# Server Configuration
# ===========================================

# Server port (default: 3000)
PORT=3000

# ===========================================
# AWS Configuration (AI for Bharat Features)
# ===========================================

# AWS Credentials
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1

# S3 Bucket for audio files (voice transcription)
AWS_S3_BUCKET=nano-cli-audio

# Bedrock Model Configuration
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0
BEDROCK_MAX_TOKENS=4096
BEDROCK_TEMPERATURE=0.7

# ===========================================
# ngrok Configuration (Web Mode)
# ===========================================

# Get your auth token from: https://dashboard.ngrok.com/get-started/your-authtoken
# Required for `luma-cli start --web`
NGROK_AUTHTOKEN=

# ===========================================
# Advanced Configuration (Optional)
# ===========================================

# CDP connection timeout in milliseconds (default: 30000)
# CDP_CALL_TIMEOUT=30000

# Snapshot polling interval in milliseconds (default: 1000)
# POLL_INTERVAL=1000
